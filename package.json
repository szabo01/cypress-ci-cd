{
  "name": "cypress-ci-cd",
  "version": "1.0.0",
  "devDependencies": {
    "cypress": "^15.1.0",
    "cypress-mochawesome-reporter": "^4.0.2"
  },
  "scripts": {
    "test": "npx cypress run --browser chrome --headless --reporter cypress-mochawesome-reporter",
    "cy:report": "npx cypress run --browser electron --headless --reporter cypress-mochawesome-reporter"
  }
}